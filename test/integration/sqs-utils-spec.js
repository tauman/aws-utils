'use strict'

// const AWS = require('aws-sdk')
// const { describe, it } = require('mocha')
// const chai = require('chai')
// const { expect } = chai
// chai.use(require('chai-as-promised'))
// const sinon = require('sinon')

// const {
//   sendMessage,
//   deleteMessage,
//   receiveMessages,
//   purgeQueue,
//   createQueue,
//   deleteQueue,
//   listQueues } = require('../../src/sqs-utils')

describe('sqs-utils.js', function() {
  describe('listQueue()', function() {
    it('tests that queues are listed')
  })

  describe('createQueue()', function() {
    it('tests that queue is created')
  })

  describe('deleteQueue()', function() {
    it('tests that queue is deleted')
  })

  describe('receiveMessages()', function() {
    it('tests that messages are received')
  })

  describe('sendMessage()', function() {
    it('tests that message is sent')
  })

  describe('deleteQueue()', function() {
    it('tests that message is deleted')
  })

  describe('purgeQueue()', function() {
    it('tests that queue is purged')
  })
})
