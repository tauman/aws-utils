'use strict'

// const AWS = require('aws-sdk')
// const { describe, it } = require('mocha')
// const chai = require('chai')
// const { expect } = chai
// chai.use(require('chai-as-promised'))
// const sinon = require('sinon')

// const {
//   createTopic,
//   deleteTopic,
//   receiveMessages,
//   listSubscriptions,
//   listSubscriptionsByTopic,
//   listTopics,
//   publish,
//   subscribe,
//   unsubscribe } = require('../../src/sns-utils')

describe('sqs-utils.js', function() {
  describe('listTopics()', function() {
    it('tests that topics are listed')
  })

  describe('createTopic()', function() {
    it('tests that topic is created')
  })

  describe('deleteTopic()', function() {
    it('tests that topic is deleted')
  })

  describe('subscribe()', function() {
    it('tests that subscription is created')
  })

  describe('unsubscribe()', function() {
    it('tests that subscription is deleted')
  })

  describe('listSubscriptions()', function() {
    it('tests that subscriptions are listed')
  })

  describe('listSubscriptionsByTopic()', function() {
    it('tests that subscriptions for a topic are listed')
  })

  describe('listTopics()', function() {
    it('tests that topics are listed')
  })

  describe('publish()', function() {
    it('tests that message is published')
  })
})
